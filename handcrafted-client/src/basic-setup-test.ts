export const simpleAddition = (num1: number, num2: number): number => {
    return num1 + num2;
}

export const simpleSubtraction = (num1: number, num2: number): number => {
    return num1 - num2;
}
